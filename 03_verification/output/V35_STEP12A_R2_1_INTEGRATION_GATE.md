# V35 Step 12A-R2.1 Integration Gate Closure

The authoritative report is generated by:

```text
python 03_verification/scripts/step12ar2_executable_model.py
```

Current status: **PASS (model-level only)**.

This gate closes the former R2 gaps: horizontal stage16 is read back before
low10 conversion; one absolute integration clock carries input, memory,
persistent R4C, and output events; A/B input ownership and full rejection are
checked; result memory uses one-cycle synchronous read response; and the event
trace records requests, responses, vector starts, kernel groups, result writes,
and output fires.

The generated companion report is
`V35_STEP12A_R2_EXECUTABLE_PROOF.md`, and the machine-readable evidence is in
`_step12ar2_audit/step12ar2_results.json`,
`step12ar2_negative_tests.json`, `step12ar2_event_trace.json`, and
`STEP12A_R2_FREEZE_MANIFEST.json`.

No wrapper RTL, V3.4 RTL, R4C RTL, ROM, golden data, or Vivado implementation
was modified in this step. Step 12B remains the next stage.
