# V3.5-17.2 STOP — Cache/Intermediate Read Timing Audit

## Scope

This was a verification-only probe. No file under `02_rtl/rtl/` was modified;
R4C and the Step12B wrapper remain frozen. No Python model was changed to make
the comparison pass. Vivado was not started.

## Baseline

- Repository base: `32d1a9591944cf92e43a1c2493c776677feb2ca0`
- Frozen R4C SHA-256: `15aa962c197c4ce0b9daf2e5478b64c51f3c6712e582f8950df6bf1c339c31b1`
- Frozen wrapper SHA-256: `1cf3d2554a6a54a4a0b0db820ffb290a80544d6c1e6c13faf1d5334b2483c584`
- `02_rtl/rtl` diff against `v3.5-17.1`: zero files

## Probe result

The normal ModelSim probe compiled and ran successfully:

```text
STEP12B_MEMORY_TIMING_PROBE_PASS
requests=8192 responses=8192 captures=8192 stage_full=128 v_writes=4096
```

The probe records the actual edge at which the current clocked RTL evaluates
the array-read RHS and updates staging. For the first vertical vector:

```text
RTL read_request       cycle 6
RTL stage_lane_capture cycle 6
RTL stage_full         cycle 21
```

The frozen Python model records:

```text
model memory_read_request  cycle 1
model memory_read_response cycle 2
model stage_capture        cycle 17
```

The important difference is relative, so it cannot be removed by one global
anchor:

```text
RTL request → lane capture = 0 cycles
model request → response   = 1 cycle
RTL first request → full   = 15 cycles
model first request → full = 16 cycles
```

The current RTL performs the read expression and the staging nonblocking
assignment in the same `posedge` block. The model explicitly schedules a
`READ_LATENCY=1` response. This is a real transaction-semantics mismatch, not
a logging offset.

## Gate decision

```text
v3.5-17.2 = STOP / NOT PASS
```

Per the frozen execution rule, this round stops here. It is forbidden to:

- alter the Python model to match the RTL;
- invent a request one cycle earlier;
- add a per-event comparator offset;
- modify the wrapper or R4C under a verification-only version;
- create `v3.5-17.2` tag;
- enter Step12C/Vivado.

The probe CSV is retained as the timing evidence. A separate design review is
required to decide whether the intended contract is the current same-edge RTL
behavior or an explicit one-cycle RAM request/response boundary. Only after
that decision may a new version change the model or datapath.

