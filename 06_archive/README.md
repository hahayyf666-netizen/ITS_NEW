# 历史归档说明

本目录保存旧阶段审计、旧原型和本地工具生成物，用于追溯，不作为 V3.5-13 当前运行入口。

- `audits/`：V1～V4、早期 Step 12 和 Step 10.2 审计。
- `prototypes/`：旧 P2A/V4 原型。
- `generated_local/`：`.Xil`、ModelSim work、Python cache 等本地生成物。
- `runtime/`：旧运行时零散记录。

整理前的完整原路径状态已冻结在 Git 标签 `v3.5-step12ar2.1-full`。若需复核旧 manifest，应从该标签恢复，避免把归档路径误当作当前路径。
